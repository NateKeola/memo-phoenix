# Memo Phoenix — Handoff

This file plus `CLAUDE.md` (the decision log especially) is the complete pickup point. A fresh session should read both before doing anything. The authoritative scope and architecture live in `docs/memo-phoenix-spec.md`; this file is the operational state on top of it.

Last updated: 2026-06-19.

## What this is

Memo Phoenix is a single-user personal knowledge and companion system. Capture (voice memo, text, interview) feeds a deterministic miner that builds a personal knowledge graph (canonical layer), which the chat, contact sheet, and companion surfaces read back. The LLM is one stage of a deterministic pipeline, not the orchestrator.

Stack: Next.js 15 App Router (single app) + a `packages/miner-core` workspace package, Supabase Postgres 17 with FORCE RLS scoped to `user_id = auth.uid()`, Anthropic SDK (`claude-opus-4-8`), ElevenLabs (Scribe STT + Conversational AI for interviews). Deploy is merge-driven: PRs to `main`, migrations apply via a GitHub Action on merge, Vercel deploys via Git integration.

## Current build state (all merged)

GitHub PR numbers do not match the spec build-sequence numbers; the mapping:

- PR #1 = spec PR0 Foundation. Scaffold (Next app + miner-core stub), single-user Supabase auth (`/login` only, no signup), full data spine, telemetry sink, FORCE RLS, migrate-on-merge CI. Migrations 0001-0007.
- PR #2 = spec PR1 Miner port. `packages/miner-core`: captures -> raw_* (one extraction LLM call per capture) -> canonical_* (Stage A people/places, B projects/events/facts, C relationships/commitments/insights). Deterministic UUIDv5 identity, id-preserving upsert, `miner_state` memoization (0008). CLI worker (`npm run miner` / `npm run seed`).
- PR #3 = spec PR2 Capture surface. The always-reachable `+`: Add text, Add memo (ElevenLabs Scribe STT, server-side), Start interview. One append-only `captures` row per capture.
- PR #4 = Canonical churn reduction. `writeCanonical` change-signature keys ONLY on `{sorted source_claim_ids, temporality}`, not summary/data drift, so re-resolution does not rewrite every row.
- PR #5 = spec PR3 Interview agent. ElevenLabs Conversational AI voice agent, two modes (open brain-dump, daily graph-aware briefing) sharing one bible system prompt. Deterministic briefing (no LLM). Migration 0009 `interview_sessions` (mutable). Each interview writes one append-only interview capture.
- PR #6 = spec PR6 Search and chat surface. `/ask`: 9 deterministic RLS-scoped retrieval tools over canonical, a thin composing LLM tool-loop, streaming, cached system prompt (padded past 4096). NO baseline gate (decision override of spec PR4). pgvector deferred.
- PR #7 = spec PR5 Contact sheet and corrections. `/people` contact sheet + rename/merge corrections (append-only `corrections` table; the miner honors them on recompute). Label rewrite before id; supersede losers; retire relationships, repoint commitments/projects/events/insights.
- PR #8 = spec PR7 Companion core (original). Today surface + draft-and-confirm Gmail/Calendar actions. Migration 0010 (`companion_state`, `google_connections`, `companion_actions`).
- PR #9 = Companion revised to conversational follow-ups, NO sending. Deleted `lib/google/*`, `app/api/google/*`, `lib/companion/draft.ts`. Migration 0011 drops `google_connections` + `companion_actions`, adds `companion_state.match_label`/`match_person_id`. Relationship-nudge heuristic, brainstorm conversation, label-drift overlay.
- PR #10 = Add context from anywhere + identity fixes (merged). Capture-with-target mechanism; first+last identity (id-preserving); the Karalea pending-rename display overlay; targeted interviews; add-context from a person / chat / follow-up; light follow-up date and linked-person tracking. Migration 0012 (`captures.target_kind`/`target_id`, `companion_state.due_date`/`linked_person_id`).
- PR #11 = Migration pipeline reliability (merged). `migrate.yml` hardened (preflight secret check, `--password`, post-push guard). New `scripts/db.mjs` is a CLI-independent migration tool over the Supabase Management API (`npm run db:status` / `db:apply`, zero deps, node:https). `docs/MIGRATIONS.md` runbook. No schema change.
- PR #12 = spec PR8 Freshness loop (merged). The self-refreshing corpus: read-time confidence decay by temporal class anchored on `last_confirmed_at` (no history churn), graph-based salience scoring, supersession driven from the model's discrepancy output, the daily-briefing reconfirm selection (replaces the stub), validity-aware retrieval (aged facts marked "as of <date>"). All in `packages/miner-core/src/freshness.ts` + `lib/freshness/decay.ts`. No migration (PR0 pre-provisioned the columns). Offline checks in `scripts/check-freshness.ts`.
- B1 = Multi-user security gate (this PR, the one in progress). NOT a feature: it proves the RLS boundary holds with two real users before any second user's data enters the DB. See `docs/SECURITY.md`. Findings: all 28 tables FORCE RLS + correct per-user policies, the one view (`reconfirm_candidates`) is security_invoker, the service-role key is server-side only, every service-role path (the miner included) scopes by user, and a live two-user test passes 30/30 with no residue. One gap found and FIXED: public signups were enabled (`disable_signup: false`); now disabled. Reusable guards committed: `scripts/check-rls.mjs` (60 assertions) and `scripts/check-multiuser.mjs` (30 assertions). No migration.
- B2 = Invite-only beta enablement (this PR, `b2-invite-beta`). The ~5-user trusted beta on top of B1. Three parts. INVITE: signups stay DISABLED; accounts are minted only via the admin API (`generateLink({type:'invite'})`, exempt from the toggle) from an operator-gated `/admin` console (`isOperator()` = `MEMO_ADMIN_EMAIL` allowlist, not roles); the invite link is shown inline (no SMTP needed) and the invited account is stamped `app_metadata {invited:true, onboarded:false}`. ONBOARDING: the middleware forces an invited-not-onboarded user to `/onboarding` (an `OnboardingInterview` reusing the interview agent in new `mode:'onboarding'` with the isolated placeholder onboarding bible); on finish `/api/onboarding/complete` sets `onboarded=true` + accepts the invite, then `/building`. MINER RUNTIME: primary is the Vercel Pro route `/api/miner/run` (`maxDuration=800`) running `mineWithLock` inline; fallback is `.github/workflows/miner.yml` (`workflow_dispatch` / `repository_dispatch`) running the CLI off-machine; `miner_runs` is the per-user concurrency lock + audit + the status `/building` polls. Migration 0013 (`invites`, `miner_runs`), both `user_id` + FORCE RLS, covered by the guards. Verified: `tsc` 0, `next build` 0, `check-rls.mjs` 64/0, `check-multiuser.mjs` 42/0, new `check-invite.mjs` 6/0, keys + bible absent from the client bundle, signups still disabled.
- Phase 2 Units 1+2 = `phase2-units-1-2` (PR open). Spec is `docs/buildout-spec-phase2.md` (Units 1-3, doneness-tagged). UNIT 1: the authored onboarding bible replaced the B2 placeholder; `npm run bible:generate` regenerated `lib/interview/onboarding-bible.generated.ts`; the onboarding path already loads it (no behavior change beyond the voice). UNIT 2: a miner-control UI over the B2 runtime (NO new plumbing, NO new table): a dedicated `/miner` page with manual "Run now" (clean `already_running` no-op), live status via the new `GET /api/miner/state`, a `miner_runs` run ledger (manual/auto/onboarding, with inserted/updated/unchanged), a progress-toward-auto-run bar, and an auto-run (default = 5 new captures since the last successful mine, the `AUTO_RUN_NEW_CAPTURES` const / `MINER_AUTORUN_NEW_CAPTURES` env) that fires through `/api/miner/run {trigger:'auto'}` with a server-side threshold re-check plus the existing lock (double-run safe). Auto-run measure + placement are Open-Exploratory: defaults chosen, options written up in the PR for Todd. Verified `tsc` 0, `next build` 0, all three guards green (64/42/6, unchanged), keys absent from the bundle.
- Phase 2 follow-up = `phase2-followup` (PR open). Three tasks, NO new table/migration. TASK A: the background auto-run threshold default is now 10 new captures (`AUTO_RUN_NEW_CAPTURES` / `MINER_AUTORUN_NEW_CAPTURES`); it governs background re-mining only, never the onboarding mine. TASK B (onboarding end experience): the interview never auto-ends; the live control is "Ready to end the interview?"; pressing it cues the agent (`sendUserMessage`) to SPEAK its warm closing recap, the session tears down only after the close finishes (auto-detected via `isSpeaking` silence, with an "End now" button + an 18s backstop that does not cut an active close), then routes to `/building?from=onboarding` ("Building your initial context"), which mines the first conversation in front of them and drops them into the populated app on `done`. The onboarding mine now always runs INLINE (`/api/miner/run` excludes `trigger='onboarding'` from the Action offload), so it builds visibly even when offload is on. TASK C (headless background mining): `app/api/cron/miner-sweep` (Vercel Cron, `vercel.json`, default hourly `"0 * * * *"`, gated by `CRON_SECRET`) sweeps all users and, for anyone over threshold with no active run, kicks off the GitHub Action per user (`triggerMinerWorkflow`, recorded `trigger='auto'`); the `mineWithLock` lock prevents any collision with an in-app run; skips cleanly if GitHub dispatch is unconfigured. Verified `tsc` 0, `next build` 0, guards 64/42/6 (unchanged), keys + `CRON_SECRET` absent from the bundle.
- Deterministic-id hardening = `id-hardening` (PR open). The root-cause fix for the four label-drift symptoms (churn, people-merge, companion-overlay drift, freshness drift). NEW MODEL: identity is a STABLE id decoupled from the mutable label. The miner resolves an extracted entity through a deterministic ladder (`packages/miner-core/src/resolution.ts`: exact label -> alias -> conservative fuzzy token-Jaccard 0.8 with an optional context key, e.g. a commitment's linked person, that relaxes/blocks) against the current canonical rows + a persisted `entity_aliases` map (migration 0014, FORCE RLS, per-user), minting a RANDOM id only on no match. A label change records a new alias, never a new id, so `companion_state` and the freshness state (which key on the canonical id) survive drift. Existing ids are kept as their stable ids (no re-key) and the resolver self-seeds from current rows, so turning it on is non-destructive. GATED behind `MINER_STABLE_IDENTITY` (DEFAULT OFF; off is identical to the old label-hash path) because the live miner cannot be exercised under the Anthropic limit; activation is the operator's deliberate cutover. SCHEMA 0014 is applied to dev; the DATA migration (`scripts/migrate-identity.mjs`, seed `entity_aliases` + report duplicate clusters) was run DRY-RUN only (read-only) and is the operator's backed-up `--apply-seed` step, NOT run on dev. Tests: `scripts/check-resolution.ts` 22/0, `scripts/check-identity-migration.mjs` 12/0; guards 66/47/6.
- Miner multi-user isolation hardening + guard = `miner-isolation-guard` (PR open; id-hardening #17 merged first). The miner runs service-role (RLS-bypassing), so its isolation is enforced by code filtering on user_id, not RLS. Audited every `.from(` in `packages/miner-core/src`: ALL queries are user-scoped, including the new resolver (`buildResolver`/`readAliasMap` filter `.eq('user_id', userId)`, so it self-seeds + resolves only the target user). Two defense-in-depth fixes: the `miner_runs` updates in `mineWithLock` now also `.eq('user_id', userId)`, and `mine()`/`mineWithLock()` call a hard `assertUserId` (uuid check) so a missing/invalid user_id throws (no unscoped run). Trigger paths confirmed single-user (route, per-user cron dispatch, Action's required+validated `user_id`); no global-mine path. New dedicated guard `scripts/check-miner-isolation.ts` (run via tsx) proves two-way isolation across captures/raw/canonical/aliases/state/runs + the real resolver cannot cross users + the hard-fail: 33/0. All four guards green; `next build` 0.
- Follow-up hygiene, time-sensitivity, and triggers = `followup-time-triggers` (PR open). MINING TRIGGERS reduced to exactly TWO: a DAILY threshold-gated cron (`vercel.json` `"0 8 * * *"`; the existing per-user sweep, mines only users >= the threshold) and the manual "Run now" button. The fire-on-app-use auto-trigger is GONE (deleted `auto-mine-on-load.tsx` + the miner-control auto-fire). TIME-SENSITIVITY: a follow-up is time-sensitive when it has a CONCRETE deadline (anti over-tagging). Inferred by the miner (commitments prompt now emits `data.deadline` ISO; regenerated) and overridable by the user via the overlay (`companion_state.time_sensitive` + the deadline reuses `due_date`), resolved read-time in `lib/companion/timing.ts` (pure, 15/0). HYGIENE: `getToday` excludes time-sensitive items whose deadline passed from the main tab and surfaces them in a read-time `past` list (never deleted); it also returns `all` for the query. QUERYABLE: the companion view has a search box + filter chips (status open/done, time-sensitive yes/no) + a person filter over `today.all`, deterministic + client-side over RLS-scoped data. New `scripts/check-timing.ts` (15/0); `check-companion` 36/0; all four guards green; migration 0015 applied to dev. The miner-inferred deadline materializes on the next real mine; the hygiene works today on the free-text `due` + user overrides.
- Lightweight + flow-hardening pass = `lean-flow-hardening` (PR open). Structural weight + flow correctness before the redesign; no restyle. DEPLOY FIX: `/api/miner/run` `maxDuration` 800 -> 300 (800 failed the Vercel deploy; only the small onboarding mine runs inline, larger/background offload to the Action). WEIGHT: code-split the ElevenLabs voice SDK with `next/dynamic(ssr:false)` so it is an on-demand chunk - `/capture/interview` 231 -> 104 kB, `/onboarding` 228 -> 104 kB First-Load JS (shared 102 -> 103 kB); interview page moved to `components/interview-widget.tsx` + a thin loader, `components/onboarding-interview-loader.tsx` for onboarding. Deleted dead `lib/supabase/client.ts`. Tightened the data layer (no behavior change): `today.ts` loads people in the parallel batch (was a waterfall), `getMinerState` parallelizes the ledger + last-done reads. FLOWS: traced all core flows (already well-hardened); fixed the today waterfall and added a home empty-state get-started hint for a brand-new user (was a bare nav). `tsc` 0, `next build` 0, all four guards green + `check-timing` 15/0, keys absent from the bundle. NOTE: the local production build wedges at the optimize step under memory pressure after repeated builds (documented); free worker/esbuild processes and retry, or rely on CI.

Spec build-sequence status (spec §16):
- PR0 Foundation, PR1 Miner, PR2 Capture, PR3 Interview, PR5 Contact sheet, PR6 Search/chat, PR7 Companion, PR8 Freshness loop: DONE.
- PR4 Baseline trainer and search gate: SKIPPED by user decision (search is always accessible).
- Beyond the spec sequence: B1 multi-user security gate (merged), then B2 invite-only beta (this PR: invite flow, onboarding/intro interview, off-machine miner runtime). Next non-beta candidates are the incremental miner pass (now higher priority: it shortens the new-user wait) and the deterministic-id hardening.

## Migrations (forward-only, in `supabase/migrations/`)

0001 helpers + telemetry; 0002 ground truth (captures, corrections, confirmations, all append-only); 0003 raw (8 tables); 0004 canonical (8 type tables + canonical_history); 0005 collections; 0006 aux (open_threads, discrepancies, reconfirm_candidates view); 0007 RLS; 0008 miner_state; 0009 interview_sessions (mutable); 0010 companion (companion_state + the now-dropped google_connections/companion_actions); 0011 drops the two sending tables, adds companion_state match columns; 0012 captures target columns + companion_state due_date/linked_person_id; 0013 (B2) `invites` (operator-owned invite audit) + `miner_runs` (per-user run lock/audit/status), both `user_id` + FORCE RLS; 0014 (id-hardening) `entity_aliases` (the alias -> stable-id resolution map, per-user FORCE RLS, service-role write) - SCHEMA applied to dev; the DATA seed is the operator's `scripts/migrate-identity.mjs --apply-seed` step, dry-run only here; 0015 (follow-up time-sensitivity) `companion_state.time_sensitive boolean` (user override; nullable; applied to dev).

NOTE (B2): `db:status` had reported 0010-0012 as PENDING even though their objects were live (applied by hand, never recorded in `schema_migrations`, and 0010 is not idempotent so re-running it would fail). B2 reconciled the bookkeeping (recorded 0010-0012 as applied, objects unchanged) and applied only the idempotent 0013. `db:status` is now clean 0001-0013 on dev.

Tables that are MUTABLE (no append-only/history trigger), because they are operational state not ground truth: `interview_sessions`, `companion_state`, `telemetry_events` (telemetry is append-only by trigger but written by service role), `miner_state`. Everything in captures/corrections/confirmations/raw_*/canonical_history is hard append-only via `forbid_mutation()`. Canonical type tables are written only by the miner (id-preserving upsert plus, in corrections, supersession).

## Repo layout (key files)

- `packages/miner-core/src/`: `run.ts` (orchestration; `mine()` plus `mineWithLock()` which wraps it in the `miner_runs` per-user lock/audit/status row), `extract.ts` (captures -> raw, honors capture target), `derive.ts` (raw -> canonical A/B/C, people pass + corrections), `corrections.ts` (rename/merge rewrite + supersede/repoint), `identity.ts` (UUIDv5, splitName/canonicalPersonId), `stage-common.ts` (writeCanonical, memoization), `anthropic.ts`, `prompts.generated.ts` (built from `prompts/*.md`).
- `lib/`: `supabase/*` (server/client/admin + `auth-admin.ts` for the service-role invite/onboarding calls), `auth/operator.ts` (operator allowlist + siteUrl), `invites.ts`, `miner/dispatch.ts` (GitHub Action trigger), `telemetry.ts`, `captures.ts`, `capture-target.ts`, `names.ts`, `people.ts`, `chat/{retrieval,agent,system-prompt}.ts`, `interview/{compose,briefing,bible.generated,onboarding-bible.generated}.ts`, `companion/{today,overlay,nudges,brainstorm}.ts`, `stt/*`, `elevenlabs.ts`.
- `app/`: `page.tsx` (home), `ask/`, `people/`, `companion/`, `capture/{text,memo,interview}/`, `admin/` (B2 invite console + actions), `onboarding/` (B2 first-run), `building/` (B2 mine status), `miner/` (Phase 2 miner-control surface), `api/{chat,companion/brainstorm,capture/memo,interview/start,interview/end,onboarding/complete,miner/run,miner/status,miner/state,cron/miner-sweep}/`, `context-actions.ts`. The headless miner cron is `app/api/cron/miner-sweep` + `vercel.json` (default hourly, gated by `CRON_SECRET`).
- Phase 2 Unit 2 files: `lib/miner/state.ts` (the new-context measure + `AUTO_RUN_NEW_CAPTURES` threshold + ledger summarize), `components/miner-control.tsx`, `components/auto-mine-on-load.tsx`, `scripts/check-miner-state.mjs` (a live measure diagnostic, not a security guard).
- `components/`: `capture-menu`, `chat`, `context-adder`, `person-corrections`, `companion/companion-view`, `admin/invite-form`, `onboarding-interview`, `building-status`.
- `scripts/`: `check-retrieval.ts`, `check-companion.ts`, `check-context.ts` (offline tsx checks); `check-rls.mjs`, `check-multiuser.mjs`, `check-invite.mjs` (live security/invite guards, zero-dep node:https).
- Prompt discipline: editing a `prompts/*.md` requires regenerating and committing the `.generated.ts` (runtime fs reads fail on Vercel). Generators: `npm run prompts:generate`, `npm run bible:generate`.

## The capture-with-target mechanism (PR #10, reusable)

A capture can be ABOUT a person, a commitment, or a chat topic: `captures.target_kind` in {person, commitment, topic} and `captures.target_id` (canonical id; null for a free topic). `lib/capture-target.ts` is the shape. The miner honors it in `extract.ts` `resolveTargetLine`: it resolves the target id to a name/label and prepends a one-line context note to the extraction INPUT only (the stored capture body is unchanged), so extracted people/facts/relationships attribute to the intended thing. Interviews additionally seed the same `DAILY_BRIEF` slot with a person or topic brief. All add-context surfaces (person `+`, chat "Talk more about this", follow-up `+`) use this one mechanism.

## Deferred items and why

1. Photo / reminisce minigame: upload a photo, get prompted to memo or talk about the trip. Its own later PR. It is the richest add-context surface and is now easy because the capture-with-target plumbing (PR #10) exists.
2. Google OAuth + connectors settings UI + single sign-on: the account-connection layer. Removed in PR #9 when the companion pivoted away from sending. A later dedicated connectors build re-introduces `google_connections` and its OAuth flow.
3. Real scheduling / sending (email send, calendar create): V1 once trusted. The companion is draft-and-suggest only; in V0 it nudges the user to act in real life and never performs an external side effect. The action gate, when built, stays in code, not a prompt.
4. System-wide deterministic name-keyed id hardening: IMPLEMENTED (branch `id-hardening`, PR open) as stable identity decoupled from the label (resolution layer + `entity_aliases`), gated behind `MINER_STABLE_IDENTITY` (default off). Remaining operator step: review the PR, run the data seed (`scripts/migrate-identity.mjs --apply-seed`, after a backup) and optionally collapse the reported duplicate clusters, then flip the flag. This is the prerequisite for the incremental miner pass.
5. Multi-user beta (B2): BUILT in this PR. Invite-only account creation (operator `/admin` console, `generateLink` invites, signups stay DISABLED), the first-run onboarding interview (placeholder intro bible, to be replaced by an operator-authored one), and the off-machine miner runtime (Vercel Pro route primary + GitHub Action fallback) all ship here. Operator config to finish wiring: `MEMO_ADMIN_EMAIL`, `NEXT_PUBLIC_SITE_URL` (prod), the miner Action's Actions secrets, and optionally `MINER_USE_GITHUB_ACTION`/`GITHUB_DISPATCH_TOKEN`/`GITHUB_REPO` + Supabase SMTP. The freshness loop's decay/salience constants are still tuned by feel; its live end-to-end is pending the first real mine after the Anthropic limit lifts.
6. Incremental miner pass: a full recompute is roughly an 8-minute cost. The current miner re-derives the whole canonical layer each run (memoized so unchanged input is a no-op, but a single new capture still re-resolves affected types). An incremental pass is deferred performance work, now HIGHER PRIORITY because it directly shortens the new-user "building your memory" wait (B2 gave the miner a home, not speed).
7. Semantic / pgvector search (spec §10 secondary path): fuzzy recall over raw for "that vague thing I said once". No pgvector, no embedding model chosen. A deliberate cost/scope decision; a later fast-follow.
8. Per-stage model-routing harness + a miner-extraction cost AB test: route different pipeline stages to different models (cheaper models for mechanical stages, stronger for resolution/insights) and AB-test extraction cost vs quality. Planned, not built.

## Known limitations and operational gotchas

- Deterministic name-keyed identity. Symptoms: (a) two genuinely-distinct people sharing an identical canonical name collide into one row; (b) label drift (the LLM rewording a canonical label) mints a new deterministic id, producing a near-duplicate INSERT, worst for phrase-labeled types (facts, insights); (c) any overlay keyed on a canonical id (companion_state on commitment id) can orphan if the label drifts. Mitigations already in place: the contact-sheet pending-rename overlay is keyed on the person id the correction targets; the companion_state overlay stores `match_label`/`match_person_id` and fuzzy re-matches a drifted commitment (person agreement plus Jaccard, with a much higher bar when no person corroborates). ROOT-CAUSE FIX IMPLEMENTED (branch `id-hardening`, PR open): stable identity decoupled from the label via the resolution layer + `entity_aliases`, gated behind `MINER_STABLE_IDENTITY` (default off) and pending the operator's deliberate cutover (apply the data seed + flip the flag). Once activated, all four symptoms retire at the source and the overlay/pending-rename mitigations become belts rather than the load-bearing path.
- Migrate-on-merge depends on three GitHub Actions secrets: `SUPABASE_ACCESS_TOKEN`, `SUPABASE_DB_PASSWORD`, `SUPABASE_PROJECT_REF`. PR #11 hardened `migrate.yml` (preflight secret check + post-push guard) and added a CLI-independent fallback: `npm run db:status` reports applied-vs-pending against the remote and `npm run db:apply` applies pending migrations, both over the Supabase Management API (node:https, no CLI, no DB password needed beyond the access token). See `docs/MIGRATIONS.md`. `.env.local` now contains `SUPABASE_ACCESS_TOKEN` + `SUPABASE_DB_PASSWORD`, so `db.mjs`, the RLS audit, and ad-hoc SQL all run locally. Never apply an empty migration. As of B1 the live dev schema is migrations 0001-0012, nothing pending.
- Local-dev environment contention: Xcode and GitHub Desktop watching the repo run concurrent git operations and can wedge git and the filesystem (hung `git` processes, stale `.git/index.lock`, `ECANCELED` read errors). Close those apps before heavy git/build work. If a commit hangs, kill the stuck `git` process and `rm -f .git/index.lock`. A file-sync tool occasionally creates spurious `"<name> 2.ts"` duplicate files that poison the local build; they are untracked junk, delete them. `rm -rf .next` before typecheck/build clears stale `.next/types/*.d 2.ts` duplicate artifacts.
- Severe environment degradation (PR #11, #12, B1 sessions): `tsc`, `tsx`, `next build`, and `git push` all wedged repeatedly (esbuild/undici/git-over-HTTPS hangs), while plain `node` + the `node:https` module worked fine. Workarounds proven: (a) verification via CI instead of local `tsc`/`tsx` (CI's `build` check is the authoritative typecheck); (b) when `git push` hangs, publish the branch via the GitHub REST API on api.github.com (create blobs/trees/commits/ref with `node:https`, then `gh api ... /pulls` for the PR) — content and messages are preserved, only commit SHAs differ; (c) the security guards (`check-rls.mjs`, `check-multiuser.mjs`) and `db.mjs` are written in zero-dependency `node:https` precisely so they run under this degradation. Global `fetch`/undici throws "Failed to parse URL" for valid URLs here, and `@supabase/supabase-js` uses fetch, so the live scripts avoid both.
- Anthropic API usage limit: blocks the miner, interview drafting, and chat answers until 2026-07-01. All deterministic surfaces (today selection, contact sheet, retrieval tools, overlays) work without the model; the model-dependent round-trips are the user's acceptance check once the limit lifts. Two real rename corrections (Kara Lee -> Karalea, Tal -> Todd) and any PR #10 targeted captures are pending the next mine.
- claude-mem Read hook: reading a file that has prior observations returns only line 1. Use Bash `cat`/`sed`/`grep` to read content, or `Read` then `Edit` (Edit works once the file is registered as read).

## Standing rules and architectural invariants (do not drift)

- Single-user tenancy: `user_id` is the tenant key. FORCE RLS on every table scoped to `user_id = auth.uid()`. No exceptions.
- The service-role key is server-side only, never in the client bundle. JWT validation on every authenticated route. Confirm secrets and prompt text are absent from `.next/static`.
- `captures`, `corrections`, `confirmations` are append-only. Every raw row carries `capture_id`; every canonical row carries `source_claim_ids`. Provenance is mandatory.
- Canonical is recomputed from the full ground-truth set every miner run. The app NEVER edits canonical directly. The miner is the only writer of canonical. Renames go through `corrections`; commitment/follow-up state goes to the `companion_state` overlay.
- Every canonical row carries a temporal class, a validity interval (`valid_from`, `valid_to`, `superseded_by`), plus `confidence`, `salience`, `last_confirmed_at`.
- The LLM is one stage of a deterministic pipeline, not the orchestrator. What to surface is chosen by deterministic code; the model is only the interview/brainstorm conversation, the on-demand chat composition, and drafting. High-stakes actions are gated in code, not a prompt.
- Deterministic prompt composition only. Direct Anthropic SDK (no LangChain). Cache system prompts (pad past 4096 tokens, ephemeral). Telemetry from day one. Prompt `.md` edits require regenerating and committing the `.generated.ts`.
- Schema changes go through migration files only; never apply an empty migration.
- Memo Phoenix is its own world (own repo, Supabase, Vercel). No path to the Miine project. The GitHub token is a fine-grained PAT scoped to this repo. The only privileged action from this environment is `gh` PR create/merge.
- No em dashes in any document or output. Direct, simple language.
- CONTINUITY: after each substantive change, append a dated entry to the CLAUDE.md decision log capturing what changed and why, AND keep this `docs/HANDOFF.md` current as part of the same change, not only at compaction time. The handoff document is a durable, continuously-updated record. Context must survive compaction and handoffs without relying on session memory.
- SECURITY POSTURE (certified in `docs/SECURITY.md`, B1): every public table has FORCE RLS + per-user policies; the one view is security_invoker; the service-role key is server-side only; the miner and every service-role path scope by `user_id` (the service-role miner path is now audited + guarded, SECURITY.md section 7); public signups are DISABLED. Re-run `node scripts/check-rls.mjs` (RLS state), `node scripts/check-multiuser.mjs` (two-user behavioral isolation of the app/RLS paths, incl. invites + miner_runs + entity_aliases), `node scripts/check-invite.mjs` (B2 invite flow), and `npx tsx scripts/check-miner-isolation.ts` (two-user isolation of the SERVICE-ROLE miner path, the one RLS does not protect) after any schema, auth, or miner change. Do not re-enable signups (B2 invites via the admin API instead).

## Immediate next steps

1. When the Anthropic API limit lifts (2026-07-01): run the miner (`npm run miner`) to fold the pending corrections (Karalea, Tal) and any PR #10 targeted captures into the graph, then do the live acceptance checks: interview round-trips (open, daily, person-targeted, chat-topic-targeted), chat answers, and companion brainstorm. Confirm Karalea propagates to the canonical label (it already shows correctly via the display overlay).
2. B2 (multi-user beta): BUILT. Operator acceptance check once the Anthropic limit lifts: invite a real address from `/admin`, open the invite link in a fresh browser, complete the onboarding interview, and confirm `/building` mines the new user's graph off-machine (Vercel route, or the Action). The onboarding bible is now the authored one (Phase 2 Unit 1).
3. Phase 2 Units 1+2 + the follow-up (threshold 10, onboarding end experience, headless cron mining): DONE (PRs open). Next is Phase 2 Unit 3 (the V1 design + performance/mobile pass: cream/ochre, no purple, mobile-first, Open-Exploratory visual directions for Todd, ideally explored in Claude Design), its own large prompt. Todd still needs to confirm the Unit 2 Open-Exploratory defaults: the auto-run measure (default new-capture count) and the surface placement (default a dedicated `/miner` page). To enable the headless cron sweep in prod: set `CRON_SECRET`, `GITHUB_DISPATCH_TOKEN`, `GITHUB_REPO` in the Vercel env (+ the miner Action's Actions secrets); cadence is in `vercel.json`. The spec also flags pulling two backlog items forward around Unit 3: deterministic-id hardening and the incremental miner pass.
3. The deterministic-id hardening (highest-value tech debt) and the photo/reminisce minigame are the next non-beta candidates now that the freshness loop and capture-with-target plumbing exist.
4. Keep every change merge-driven: branch off main, commit in logical steps, open a PR with `gh`, do not self-merge unless asked. Append a decision-log entry each time.
