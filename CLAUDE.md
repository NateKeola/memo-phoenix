# CLAUDE.md — Memo Phoenix

Single-user personal knowledge and companion system. Capture (voice memo, text, interview) feeds a miner that builds a personal knowledge graph, which a companion layer reads. The source of truth for scope and architecture is `docs/memo-phoenix-spec.md`. Read it before building. Build the V0 surface of a feature, then stop. V0 means exists and works, not polished.

## How to read the spec
- LOCKED items are constraints. Implement as written.
- OPEN-EXPLORATORY items: implement the recommended option and note the assumption in the PR description. Do not stall.
- If something contradicts a LOCKED item or is ambiguous at the architecture level, ask before building.

## Standing rules (every session)

### Data and security
- Single-user tenancy: `user_id` is the tenant key. RLS on every table, scoped to `user_id`. No exceptions.
- Service role key server-side only. Never in client code or the browser bundle.
- JWT validation on every authenticated route. Principle of least privilege everywhere.
- `captures`, `corrections`, `confirmations` are append-only. Every raw row carries `capture_id`. Every canonical row carries `source_claim_ids`. Provenance is mandatory.
- Canonical is recomputed from the full ground-truth set (raw + corrections + confirmations) on every run. Never edit canonical directly.
- Every canonical row carries a temporal class (evergreen | dated | decaying), a validity interval (`valid_from`, `valid_to`, `superseded_by`), plus `confidence`, `salience`, `last_confirmed_at`.
- Schema changes go through Supabase migration files only. No manual dashboard schema edits. Never apply an empty migration.

### Architecture
- The LLM is one stage of a deterministic pipeline, not the orchestrator. Routing goes in code wherever code can decide it.
- Hard-gate high-stakes actions (sending email, scheduling, anything with an external side effect) in code, not in a prompt instruction.
- Deterministic prompt composition only. No LLM meta-prompt.
- Direct Anthropic SDK. No LangChain.
- Cache system prompts (pad past 4096 tokens, mark ephemeral). Telemetry from day one: tool calls, miner runs, cache hit rate.
- Prompt template `.md` edits require regenerating and committing the `.generated.ts` files. Runtime filesystem reads fail in Vercel serverless.

### Separation and credentials
- Memo Phoenix is its own world: own repo, own Supabase account or org, own Vercel project. There is no path to the Miine canonical Supabase project. Do not reference or connect to it.
- The GitHub token is a fine-grained PAT scoped to this repo only.
- Merge-driven model: the only privileged action from this environment is `gh` PR create and merge. Once wired, migrations apply via a GitHub Action on merge to main, and Vercel deploys via the Git integration. Platform tokens (Supabase, Vercel) live in GitHub Actions secrets, not in this environment. During bootstrap (PR0 and PR1) applying migrations directly to the dev project with `supabase db push` is acceptable.

### Writing
- No em dashes in any document or output.
- Direct, simple language. No formatting flourishes.

## Decision log
Append a dated entry after every substantive decision.

- 2026-06-16: Project bootstrapped. Spec v0.1 locked. Build follows spec §16 PR sequence. Structure is a single Next.js app plus a `packages/miner-core` package. Freshness model is the self-refreshing corpus: validity intervals and supersession, decay by temporal class, re-confirmation folded back through the interview loop, salience-gated. Companion actions are draft-and-confirm in V0. Tenancy is single-user via `user_id` with RLS retained.
- 2026-06-16: PR0 (Foundation) built. npm-workspace scaffold (single Next.js App Router app plus `packages/miner-core` stub), single-user Supabase Auth (`/login` only, no signup UI), full data spine (captures/corrections/confirmations, 8 `raw_*`, 8 `canonical_*` with the mandatory shared block, collections, aux), `reconfirm_candidates` computed view, `telemetry_events` sink, append-only via `forbid_mutation()` triggers, canonical history via `snapshot_canonical()`, FORCE RLS on every table scoped to `user_id = auth.uid()`, migrate-on-merge CI. OPEN-EXPLORATORY lean options taken: #2 no pgvector in PR0, #3 loose canonical columns, #5 no decay constants in DDL, #8 corrections/confirmations separate, #9 reconfirm view computed, #10 single app plus one package. `superseded_by` is a plain uuid with no FK (a self-FK would fight the nightly recompute).
- 2026-06-16: Migrate workflow secrets (`SUPABASE_ACCESS_TOKEN`, `SUPABASE_DB_PASSWORD`, `SUPABASE_PROJECT_REF`) must be set as GitHub Actions secrets before PR0 merge. Otherwise the first `migrate.yml` run fails with a harmless red X, since the PR0 schema is applied to dev by direct `supabase db push` during bootstrap.
- 2026-06-16: Forward (do not build now): if `migrate.yml` is ever pointed at a production project, gate it behind a GitHub Environment with required approval. The dev project keeps auto-applying.
- 2026-06-16: PR1 forward note A: `captures` is hard append-only via `forbid_mutation`, so any "already processed" state the miner needs lives in a SEPARATE table, never as a column on `captures`.
- 2026-06-16: PR1 forward note B: canonical recompute in PR1 is an id-preserving UPSERT, not delete-and-reinsert. Delete/reinsert would write a full snapshot of every canonical row to `canonical_history` every night (burying real history in recompute churn) and would break `superseded_by`, since ids would not survive. Preserve ids, update in place.
- 2026-06-17: PR1 (Miner port) built in `packages/miner-core`. Pipeline: `captures` -> `raw_*` (one extraction LLM call per capture emits the 8 working-set sections) -> `canonical_*` via Stage A (people, places_orgs) -> B (projects, events, facts) -> C (relationships, commitments, insights), one LLM call per canonical table. Runs as a CLI worker (`npm run miner`, `npm run seed`) using the service-role client; the LLM is one stage, code orchestrates. db.json section shapes settled: people{name,aliases,relationship,role,work_or_personal}, places_orgs{name,kind,role,work_or_personal}, projects{name,status}, events{title,date,location,participants}, facts{fact,category}, relationships{source,target,relation}, commitments{what,due,person,work_or_personal,status}, collection_mentions{item,collection}. Canonical `data` stays loose per open decision #3 (relationship endpoints, commitment status, insight fields all live in `data`).
- 2026-06-17: PR1 exploratory choices. Identity is deterministic UUIDv5 over (user_id, canonical_table, normalized_label), so re-mining is id-preserving without name-matching machinery (the spec/§4.1 call; Miine used random ids + name match). Recompute is full-recompute with two memoization layers in a new `miner_state` table (forward note A: separate from append-only `captures`): per-capture extraction marker and per-canonical-type input hash, so a second run over unchanged input does zero LLM work and writes nothing (no `canonical_history` churn). Within a changed type, only rows whose content signature differs are upserted. Model is `claude-opus-4-8` with adaptive thinking + cached system prompts (env-overridable via MINER_MODEL/MINER_EFFORT). JSON via prompt-instructed-JSON + strip-fences + retry (not strict structured outputs, because `data` is free-form). salience is provenance-weight only for V0; temporality is classified per type with per-node LLM override. Aux tables (open_threads, discrepancies) and canonical collections derivation are DEFERRED past PR1 (counts collected for telemetry only). Stage A validation against a real capture is the gate and is pending the user's `.env.local` creds + transcript.
- 2026-06-17: PR1 adversarial self-review (could not run the miner locally: placeholder dev creds, no ANTHROPIC_API_KEY) caught and fixed 5 bugs before PR: extraction memo hash must cover mode/modality not just body; `writeCanonical` existence read must filter `valid_to is null`; node and relationship id-collision merges must union `data`/aliases not just provenance; empty-text LLM responses now raise a clear error.
- 2026-06-17: PR1 Stage A gate PASSED on the user's real brain-dump (capture b53f7f2a): 91 raw -> 99 canonical nodes/edges/insights, all with provenance; per-node temporality (facts 18 evergreen + 1 decaying; commitments 1 dated + 1 decaying); second run a 4s no-op via miner_state (canonical_history stayed 99, no churn); 9 miner_run telemetry events/run. Known limitation: deterministic UUIDv5 identity collides two genuinely-distinct people sharing an identical canonical name into one row (no such pair in this transcript; harden around PR5 alias resolution if it arises).
- 2026-06-17: PR2 (Capture surface) built. Three distinct flows behind an always-reachable `+` on the home surface: (1) Add text -> server action writes a `mode='text'` capture with optional `routing_hint`; (2) Add memo -> browser MediaRecorder posts audio to a server route `/api/capture/memo` that transcribes via ElevenLabs Scribe and writes a `mode='memo', modality='voice'` capture, showing the transcript; (3) Start interview -> a `coming in PR3` stub, no agent. STT interface: `SttProvider.transcribe(audio: Buffer, contentType): Promise<{text}>` (`lib/stt/types.ts`), one Scribe impl (`lib/stt/elevenlabs.ts`, raw multipart POST to api.elevenlabs.io/v1/speech-to-text, model_id env-overridable default `scribe_v1`), swapped via a single line in `lib/stt/index.ts`; all `server-only`. Audio format settled: whatever MediaRecorder emits (Chrome: audio/webm;opus), sent as the request body with its MIME type; the filename hint maps the container; Scribe auto-detects. Every successful capture logs a `capture` telemetry event (mode, modality). Key isolation verified: ELEVENLABS_API_KEY value + vendor strings absent from the client bundle.
- 2026-06-17: PR2 exploratory choices. Audio file retention (`audio_url`) is DEFERRED in V0 (transcript-only; no Storage bucket); note it for later if memo playback is wanted. The shared middleware now excludes `/api` from the page redirect so API routes self-handle auth and return JSON 401 (the session is still refreshed). No SDK dependency added for ElevenLabs (raw fetch). The live in-browser UI -> capture -> miner round-trip is the user's acceptance check (cannot drive an authenticated browser headlessly, and `captures` is hard append-only so synthetic test rows cannot be cleaned up); build + dev smoke + key-isolation verify the surface here.
- 2026-06-17: PR2 capture surface validated live on real user captures (text + memo). The Add memo flow transcribed a real voice memo via ElevenLabs Scribe (modality=voice); 3 capture telemetry events logged; the miner picked up the 3 new captures incrementally (the prior one skipped via miner_state) and resolved new entities (e.g., the fact "Cole and Karalea have a dog named Sukah"). "Karalea" correctly resolved to existing "Kara Lee" (no duplicate person).
- 2026-06-17: Canonical-churn reduction (follow-up PR off main, `reduce-canonical-churn`). The miner's change-detection in `writeCanonical` (stage-common.ts) now keys ONLY on the meaningful resolution: `changeSignature` = {sorted source_claim_ids, temporality}. The synthesized/cosmetic fields (summary, data wording, confidence, salience, label casing) are excluded, because the LLM rewords them on every re-resolution and the old full-row signature counted that as a change, rewriting every row and flooding canonical_history. A row now updates only when its claims or class actually change. Env flag MINER_CHURN_DEBUG=1 logs an A/B per table (provenance-sig writes vs old full-sig would-write). Demonstrated on a forced full re-resolution of the unchanged corpus: cosmetic UPDATES dropped from ~114 to 1 (people/places/events/commitments: 0 rewrites; history grew +18 vs +131); a second run is a clean no-op; people stable with no duplicates. RESIDUAL (separate issue, NOT fixed here): label drift still causes INSERT churn (the LLM rewording a canonical label mints a new deterministic id -> near-duplicate row), worst for phrase-labeled types (facts/insights). That is the deterministic UUIDv5-by-normalized-label limitation; address it (alias/claim-overlap matching, or stabler labels) before PR8.
- 2026-06-17: PR3 (Interview agent) built on branch `pr3-interview-agent`. The PR2 stub becomes a real ElevenLabs Conversational AI voice agent with two modes sharing ONE system prompt (the bible): open brain-dump and daily graph-aware briefing. The bible is a user-authored file `packages/miner-core/prompts/memo-companion-bible.md` (238 lines) with placeholders `{{user_name}}`, `{{DAILY_BRIEF}}`, `{{CURRENT_DATE_TIME}}`; it is bundled to `lib/interview/bible.generated.ts` by a new `npm run bible:generate` script (same `.md -> .generated.ts` discipline as the miner prompts, since runtime fs reads fail on Vercel). `lib/interview/compose.ts` fills the placeholders and picks the first message. Each interview writes exactly ONE append-only `captures` row at end (`mode='interview'`, `modality='voice'`, `interview_id` = session), so the miner folds an interview into the graph on its next run exactly like a memo or text capture. New migration `0009_interview_sessions.sql`: a `interview_sessions` table (mode, brief jsonb, elevenlabs_conversation_id, started_at, ended_at) with FORCE RLS and select/insert/update policies. Unlike every other table it is MUTABLE (no `forbid_mutation`/history trigger): a session is opened then closed, it is operational state, not ground truth.
- 2026-06-17: PR3 briefing is DETERMINISTIC code, no LLM (`lib/interview/briefing.ts`, server-only, RLS-scoped reads). `composeBrief` reads canonical for open commitments (status != done), recent people/projects/events (created_at desc), plus a resurfacing STUB (the 2 oldest-by-salience people). This was the planned shape (recency real, resurfacing stubbed until the freshness loop in PR8), and it also sidesteps the Anthropic API usage limit (regain 2026-07-01) that currently blocks the miner: a code briefing needs no model call, so daily mode works today. Daily mode "degrades" to open behavior when the brief is empty (thin graph or a briefing error): the bible reads an empty `{{DAILY_BRIEF}}` as open mode, so the route computes an `effective_mode` and sends the matching first message. Verified against the live graph: 13 real brief items (College Tour follow-up, Kareem, Justin Keating, Jacob's restaurant, plus Brian/Morgan resurfaced).
- 2026-06-17: PR3 ElevenLabs wiring. The signed WebSocket URL is minted SERVER-side (`lib/elevenlabs.ts`, `getSignedUrl`, `xi-api-key` header), so `ELEVENLABS_API_KEY` never reaches the browser (key isolation grep confirms it, the service-role key, and the bible text are all absent from `.next/static`). The per-session system prompt and first message are applied CLIENT-side as `conversation_config_override` at `startSession` (matches the reference; the SDK only supports overrides client-side). This REQUIRES enabling the agent's "System prompt" and "First message" override toggles in the ElevenLabs dashboard (Security tab); without them ElevenLabs rejects the session. Documented in the README and surfaced as an inline error hint. At end the authoritative transcript is fetched server-side from ElevenLabs (retry while status is still processing), with the client-accumulated transcript as a fallback; a too-short conversation (< 2 turns or < 20 chars) is not captured.
- 2026-06-17: PR3 exploratory choice: the composed system prompt (bible + the user's own brief) is returned to the user's own browser to apply the override. Judged acceptable: it is the user's own data, it must reach ElevenLabs for the agent to use it regardless, and single-user tenancy means it crosses no boundary. If a multi-tenant or stricter posture is ever wanted, mint the override server-side instead (would require an ElevenLabs API path that accepts the override at mint time). Diagnostics for the three likely trouble spots (signed-URL mint, override permission, end-of-session transcript) are folded into this PR: explicit errors on signed-URL/JSON-parse failure, a connection timeout that hints at the dashboard toggles, a getUserMedia NotAllowedError message, and authoritative+fallback transcript capture. HANDOFFS: apply migration 0009 to the dev project; enable the two dashboard override toggles; set `NEXT_PUBLIC_ELEVENLABS_AGENT_ID` and `MEMO_USER_NAME`; the live in-browser voice round-trip is the user's acceptance check (cannot drive an authenticated mic'd browser headlessly); folding a captured interview into the graph is blocked by the Anthropic API limit until 2026-07-01.
- 2026-06-18: Search and chat surface (spec §16 PR6) built on branch `search-chat-surface`. The user reads the graph back for the first time: an `/ask` chat box (linked from home) where a question streams a grounded answer. DECISION OVERRIDING THE SPEC: no baseline gate (spec §8). The user chose to skip the trainer/coverage gate so the corpus can be queried freely, so the surface is always accessible to the signed-in user; the baseline trainer was not built. Out of scope and DEFERRED: semantic/fuzzy recall over raw via pgvector + embeddings (the §10 secondary path; deliberately a later fast-follow, no pgvector, no embedding model chosen here), the contact sheet/people page/alias-merge (next PR), and the companion's nudges/actions.
- 2026-06-18: PR6 retrieval is the §10 primary path: structured retrieval over canonical, never re-deriving from raw. `lib/chat/retrieval.ts` is 9 deterministic, narrowly-named tools (each a plain DB query, no LLM): get_person, get_project, find_commitments, list_upcoming, search_facts, neighbors_of, list_recent, list_in_collection, get_provenance. Every query is scoped to the signed-in user TWO ways: the request's RLS-bound anon client AND an explicit `.eq('user_id', userId)` (defense in depth, and so the same code is offline-testable with a service-role client scoped to one user). The corpus is small and single-user, so tools fetch the current rows of one type (`valid_to is null`, salience-ordered) and rank/filter in code (token-overlap scoring, alias-aware name match so "Karalea" resolves to "Kara Lee") rather than pushing fuzzy jsonb matching into SQL; the whole graph never enters the model prompt, only the matched compact subset. Validity-aware: tools return current rows and the validity fields; project() emits `current:false` + `as_of` and the system prompt annotates aged rows, but that path is currently unreachable because tools filter to current only (no supersession until PR8). Provenance chain: canonical `source_claim_ids` -> a row in one of the 8 `raw_*` tables -> its `capture_id` -> `captures(mode, created_at)`; resolved across all raw tables (an insight's claims span types). Each factual row carries an inline `provenance` hint ("from your interview on <date>"), plus get_provenance for the x-ray.
- 2026-06-18: PR6 composing stage (`lib/chat/agent.ts`, server-only) is a thin LLM loop: claude-opus-4-8, adaptive thinking, cached system prompt (env-overridable CHAT_MODEL/CHAT_EFFORT/CHAT_MAX_TOKENS). The model routes to the tools and composes; it never gets the graph. Loop hardening (from the adversarial review): tools execute whenever the turn carries tool_use blocks, decoupled from stop_reason (a truncated routing turn no longer silently drops its tool call); on `max_tokens` the answer is marked truncated; iteration-cap exhaustion no longer returns empty (a final tool-free `tool_choice:none` turn composes from what was gathered, else a graceful fallback). Streaming: ONLY the final answer is streamed (chunked) to the client, so intermediate tool-routing turns never leak preamble into the answer and the streamed bytes equal the recorded text. `app/api/chat/route.ts` (nodejs) is auth-gated, streams plain text via a ReadableStream, normalizes the recent turns (no durable chat storage), and degrades to a friendly message on a model error (e.g., the current usage-limit 429). Telemetry via the PR0 sink: one `tool_call` event per invocation (surface=chat) and one `chat_query` per question (tools, iterations, cap_hit, truncated, usage, cache_hit), so "answers built from targeted tool calls" is verifiable in the log.
- 2026-06-18: PR6 exploratory choices and verification. The cached system prompt is padded to a real tool-routing rubric + data-shape reference + worked examples; count_tokens confirms 4198 tokens for the system block alone (tools push the cached prefix to ~5.3k), clearing the LOCKED 4096 so cache_control ephemeral actually fires on Opus 4.8 (the standing rule). The final answer is CHUNK-streamed after composition, not token-streamed from the model: a deliberate V0 choice so no intermediate-turn text can ever pollute the answer; true token-streaming of the final turn is a V1 nicety. Verification was done OFFLINE against the real graph (no model call, so it runs under the Anthropic usage limit, regain 2026-07-01) via `scripts/check-retrieval.ts`: the four canonical questions ("what am I working on", "who is Karalea", "what do I owe people", "what is coming up") answer correctly from real rows with provenance; "Karalea" -> "Kara Lee"; find_commitments(person) returns the named person's commitments (filtered before the cap); get_provenance resolves a claim to its interview capture; RLS proof (anon, no session, sees 0 canonical rows); key isolation (ANTHROPIC_API_KEY, the service-role key, and the system prompt text all absent from the client bundle). The live chat answer (the model loop) is the user's acceptance check once the API limit lifts; the deterministic surface (tools, RLS, telemetry, provenance) is fully verified now. A 6-dimension adversarial review (each finding double-verified) confirmed and fixed 8 findings: intermediate-turn text pollution + telemetry mismatch, find_commitments person-filter-after-cap, iteration-cap empty answer + wasted final batch, max_tokens silent truncation/tool-drop, cached prompt below 4096, and listInCollection unescaped ilike wildcards.
- 2026-06-18: Contact sheet + people corrections (spec §16 PR5) built on branch `contact-sheet-corrections`. The first correction capability in the app, and it fixes the PR1 label-keyed-identity split limitation. NO new migration: the `corrections` table is from PR0 and the loser supersession reuses the existing `valid_to`/`superseded_by` columns. Contact sheet: `/people` (list) and `/people/[id]` (detail with relationships, commitments, provenance), RLS-scoped, reusing the chat retrieval helpers (`neighborsOf`, `resolveProvenance`) in `lib/people.ts`. Corrections write an append-only `corrections` row ONLY (kinds `rename_person` {from_label,to_label}, `merge_people` {from_label,into_label}); server actions in `app/people/actions.ts` never touch canonical; a `correction` telemetry event logs each issue. The UI states plainly that a correction applies on the next miner run.
- 2026-06-18: PR5 miner honors corrections (`packages/miner-core/src/corrections.ts` + `derive.ts`). KEY INSIGHT: rename and merge are the SAME operation, a label rewrite applied BEFORE the deterministic id is computed. `buildPeopleRewrite` reads corrections in created_at order into a normalized-from-label -> final-survivor-label map (chained to a fixpoint, cycle-guarded) and a loserId->survivorId map; its fingerprint is folded into the people pass inputHash so a new correction busts the memo. In the people pass, each resolved node name is rewritten to the survivor label before `canonicalId`, the original surface form is kept as an alias, and the existing same-id collapse unions provenance, so every mention of the old name reroutes onto the survivor id on every recompute (id-preserving, no duplicate). `supersedeLosers` retires the stale loser person row (valid_to + superseded_by=survivor) once, BEFORE downstream context is read, so B/C never see it. The miner is still the only canonical writer; canonical is mutable by the miner (only ground-truth/raw/history carry forbid_mutation).
- 2026-06-18: PR5 downstream-reference cleanup is SPLIT by how the row is keyed, a distinction the adversarial review surfaced. Relationship edges are keyed on their endpoints (id = hash of source|target|relation), so a merge's re-resolution emits the survivor edge under a NEW id (inserted) and the stale loser edge is RETIRED (`retireStaleRelationships`). Commitments, projects, events, and insights are keyed on their own label, so re-resolution reuses the SAME id and `writeCanonical`'s change-signature (provenance + temporality only, data excluded for churn control) SKIPS the data-only person-ref change, leaving a dead loser id embedded; these are REPOINTED in place (`repointReferences` rewrites commitment.person_id, project/event.related_ids, insight.affected_entity_ids loser->survivor, deduping arrays). Retiring these instead would silently DELETE a commitment with no replacement (the review's highest-severity finding). All cleanup is idempotent (no loser refs left after the first apply) and a zero-cost no-op when there are no corrections.
- 2026-06-18: PR5 verification and exploratory choices. The live graph already has "Karalea" unified into "Kara Lee" as an alias (the resolution LLM handles that case), so there is no live duplicate pair to merge; the rename/merge machinery is therefore validated DETERMINISTICALLY (no model call) in `scripts/check-corrections.ts` (21 passing: the Karalea worked example, merge, chained fixpoint, cycle/no-op guards, cleanup no-op safety, and a read-only preview confirming a "Kara Lee" merge would retire 2 relationships and repoint 1 insight). The end-to-end (issue a correction, run the miner, see the corrected graph) is the user's acceptance check once the Anthropic usage limit lifts (regain 2026-07-01). Exploratory choices: losers are SUPERSEDED (valid_to + superseded_by) not deleted, which is spec-aligned (validity intervals), reversible, and preserves history; downstream label-keyed refs are repointed in place rather than forced through writeCanonical (keeps the churn-reduction signature intact). DEFERRED (noted): the split inverse (one row that is actually two people, force a split) is not built; it needs claim-level splitting, not a label rewrite, so it is a later PR. The 6-dimension adversarial review (each finding double-verified) confirmed and fixed 5 findings (commitment-delete-on-merge, projects/events dangling related_ids, insight repoint, non-person relationship view-links 404, unordered "first mentioned" provenance) and rejected 3 low ones.
- 2026-06-18: Companion core (spec §16 PR7) built on branch `companion-core`. The first surface where Memo acts on your life: a `/companion` today view that surfaces open commitments and due items, and draft-and-confirm Gmail/Calendar actions. New migration `0010_companion.sql`: `companion_state` (mutable overlay), `google_connections` (server-only tokens), `companion_actions` (append-only audit of sends). Migration applies on merge, so the surface degrades gracefully when the tables are absent.
- 2026-06-18: PR7 today surface (`lib/companion/today.ts`, `app/companion/page.tsx`) is DETERMINISTIC, no model call: it reads the user's current canonical commitments and events (RLS-scoped), overlays `companion_state`, and groups overdue/soon/open via a best-effort `bucketOf` over the free-text dues (informal dues like "tomorrow"/"the 20th" can't be parsed precisely, so it recognizes common signals and the real `snooze_until` timestamp, and never hides an item, only orders it). Provenance reuses the chat surface's `attachProvenance` (exported for this). COMMITMENT STATE choice: `companion_state` is a MUTABLE overlay (chosen over a corrections-style row that the miner honors). Rationale: done/snooze/dismiss is operational UI state, not ground truth about the user's life; an overlay keeps it out of the LLM recompute entirely (no churn), the miner never touches it so it survives recompute trivially, and the surface reads it alongside canonical with the overlay winning. Keyed on the canonical commitment id. KNOWN LIMITATION (review-surfaced, deferred): the id is deterministic UUIDv5 over the commitment label, so if a label drifts the id changes and the overlay orphans (a resolved item could resurface); same deterministic-id limitation flagged for canonical churn and people-merge, to be addressed with stabler labels / the freshness loop.
- 2026-06-18: PR7 draft-and-confirm is the high-stakes-action gate IN CODE, not a prompt instruction (harness doctrine). Drafting and sending are SEPARATE server actions (`app/companion/actions.ts`). `draftEmailAction`/`draftCalendarAction` call the model (`lib/companion/draft.ts`) to draft CONTENT only. `sendEmailAction`/`createEventAction` perform the Google REST call, gated on `input.confirm === true` AND a live connection, and NEVER import or call the model. There is no path from model output to a send: the model cannot reach the send functions, and a send requires the user to invoke a distinct action after reviewing/editing the draft. Each send is recorded in `companion_actions` (audit; no secrets in the payload). The structural check (`scripts/check-companion.ts`) asserts draft.ts cannot import gmail/calendar/connection and the send functions never reference the draft model.
- 2026-06-18: PR7 Google integration is server-side OAuth + raw fetch (no SDK), least privilege: scopes `gmail.send` (send only, cannot read mail) and `calendar.events`. Tokens live in `google_connections` with FORCE RLS and NO policies, so only the service-role admin client (server-side `lib/google/connection.ts`) ever reads/writes them; they never reach the browser (verified: client secret, token values, and all googleapis URLs absent from `.next/static`; the `access_token`/`refresh_token` strings in the bundle are the ElevenLabs and Supabase auth field names, not Google tokens). Routes: `/api/google/connect` (consent with an httpOnly state cookie for CSRF, `access_type=offline` + `prompt=consent` for a refresh token), `/api/google/callback` (verify state, exchange code, store tokens, refresh_token preserved when Google omits it on re-consent), `/api/google/disconnect`. Graceful degradation: when `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` are unset or the user is not connected, the today view still works and prompts to connect; the send actions return `needsConnect`. Telemetry: `companion_surfaced` (counts), `companion_state` (new state), `companion_draft` (kind), `companion_send` (kind + status). The 6-dimension adversarial review (each finding double-verified, focused on the send-gate and token isolation) confirmed ZERO findings. HANDOFFS: apply migration 0010 on merge; set `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` and register the redirect URI + enable the Gmail/Calendar APIs in Google Cloud; live drafting is blocked by the Anthropic usage limit until 2026-07-01; the live connect + draft + send round-trip is the user's acceptance check.
- 2026-06-19: Companion REVISED to conversational follow-ups (branch `companion-conversational`, off main after PR8 merged). DIRECTION CHANGE: the companion helps the user stay connected with the people in their life and nudges them to reach out in plain language; it does NOT send email or create calendar events in V0. The PR8 sending layer is removed: deleted `lib/google/*`, `app/api/google/*`, `lib/companion/draft.ts`; migration 0011 DROPS `google_connections` and `companion_actions` (both unused, safe). Sending (Gmail/Calendar), Google OAuth, and single sign-on are DEFERRED to a later settings/connectors build (planned, not built here). The miner-side root-cause deterministic-identity fix is also a separate dedicated PR.
- 2026-06-19: Revised companion surface (`lib/companion/today.ts` + `nudges.ts` + `app/companion/page.tsx`) is DETERMINISTIC, no model for selection. Commitment follow-ups are grouped overdue/soon/open and phrased as plain-language suggestions (`phraseCommitment`, e.g. "You said you'd go spearfishing with Kolton Lye, in a couple weeks"), with provenance. Relationship nudges (`relationshipNudges`) use a SIMPLE, transparent recency heuristic: closeness weight (>= 2) from the relationship/closeness fields (NOT the name, and pure work ties like "business partner" excluded), then least-recently-mentioned (via the claim -> capture-date chain), then fewest mentions. This is a documented placeholder; the real decay-and-salience scoring is the freshness loop (a later PR). The only model calls in this surface are the on-demand brainstorms.
- 2026-06-19: Revised companion brainstorm (`lib/companion/brainstorm.ts` + `app/api/companion/brainstorm/route.ts`): an on-demand short conversation about one follow-up. It reuses the chat composing loop (`runChat`, now takes an optional `systemPrompt`) with a companion brainstorm system prompt and the EXISTING READ-ONLY retrieval tools, so the model has graph access. It suggests small real-life next steps and copyable text; it does not and CANNOT send (there is no send capability; the prompt is framing, not the guard). Server-side, streams plain text, telemetry `companion_brainstorm`. Commitment state stays in the mutable `companion_state` overlay (`setCommitmentState`, RLS-scoped, never canonical, untouched by the miner). LABEL-DRIFT FIX (the required local fix): migration 0011 adds `match_label` + `match_person_id`; `setCommitmentState` stores the label + linked person at write time; `lib/companion/overlay.ts` `matchOverlay` re-associates a commitment that re-resolved under a new id by exact id first, then person agreement plus a fuzzy label match. The adversarial review (6-dimension, each finding double-verified) hardened the fuzzy match: with no person to corroborate, a label-only re-association now needs a much higher Jaccard (0.8) than a person-corroborated one (0.5), so a done/dismissed state cannot be carried onto a different person-less commitment with weak overlap (which would silently hide a live follow-up). 3 findings confirmed (all this loose match) and fixed; 7 rejected, several heuristic-quality ones (name-keyword matching, business-partner-as-family, acronym phrasing, zero-mention phrasing) addressed defensively anyway. Palette is a warm placeholder (cream + ochre, no purple), plain inline styles, not a theme layer (real styling is V1). Verified with 36 offline checks (no model, no send): sending layer gone, label-drift re-match across all cases, heuristic sanity, phrased follow-ups, brainstorm server-side; build green; no secrets or prompt text in the client bundle. HANDOFFS: migration 0011 applies on merge; live brainstorm is blocked by the Anthropic usage limit until 2026-07-01; the live brainstorm + overlay round-trip is the user's acceptance check.
- 2026-06-19: Add context from anywhere + identity fixes (branch `add-context-anywhere`). CAPTURE-WITH-TARGET (the reusable connective tissue): a capture can be ABOUT a person, a commitment, or a chat topic. Migration 0012 adds `captures.target_kind` ('person'|'commitment'|'topic') + `captures.target_id` (the canonical id, null for a free topic); captures stays append-only (set at insert only). `lib/capture-target.ts` is the shape + `parseTarget`; `writeCapture` accepts it; the miner HONORS it in `extract.ts` (`resolveTargetLine` resolves the target id to a name/label and PREPENDS a one-line context note to the extraction INPUT only, never modifying the stored capture body, so extracted people/facts/relationships attribute to the intended thing) with `run.ts` selecting the columns. One mechanism, used by all three surfaces.
- 2026-06-19: Task 1a first+last identity. `identity.ts` adds `splitName` (first token / rest, single token -> last empty), `personKey(first,last) = normalize(first + ' ' + last)`, and `canonicalPersonId`. Because joining first+last reconstructs the label for a token split, canonicalPersonId EQUALS the prior canonicalId-by-label for existing people: the identity basis becomes first+last with ZERO id churn (verified: canonicalPersonId == old label id for every real person, no duplicates, no orphans). The people pass (`derive.ts`) routes person ids through canonicalPersonId and stores `data.first_name`/`last_name`; `corrections.ts` `buildPeopleRewrite` computes loser/survivor ids through the SAME helper (the lockstep the Karalea investigation flagged, so a single-token rename like Karalea -> first="Karalea", last="" supersedes the exact id the people pass mints). The app displays first/last via `lib/names.ts` (`firstLast` prefers the miner-persisted data, else splits the label, so it works before the next mine).
- 2026-06-19: Task 1b Karalea. ROOT CAUSE (read-only investigation across corrections.ts -> derive.ts -> stage-common.ts -> lib/people.ts): the rename pipeline is CORRECT and would propagate on the next mine (rewrite -> insert survivor -> supersede loser -> display reads valid_to is null); there is NO code or display bug in the single-label path. It had not propagated only because the miner is blocked by the Anthropic API limit, so the confirmed correction sits pending. FIX = a DISPLAY-ONLY pending-rename overlay in `lib/people.ts` (`pendingRenames` + `applyPending`) so a confirmed rename reaches the contact sheet immediately without a mine and without editing canonical. It is keyed on the SPECIFIC person id the `rename_person` correction targets (payload.person_id), so it cannot re-fire onto a different later person who reuses the old name, and it self-consumes once the mine applies the rename and the person's id changes. Merges are NOT shown optimistically (relabeling the loser pre-mine would show two same-named rows). Demonstrated live: the People list now shows "Karalea" (flagged "rename pending next sync"), no stale "Kara Lee", no duplicate.
- 2026-06-19: The interview injection reuses the briefing mechanism: `app/api/interview/start` accepts a target and seeds the SAME `DAILY_BRIEF` slot with `composePersonBrief` (the named person + what is known, RLS + explicit user_id scoped) or `composeTopicBrief` (the chat topic); `end` carries the target into the capture; the interview page threads the target through URL params. Task 2 (add context from a person): a `+` `ContextAdder` on the People list and detail offers a text note (`app/context-actions.ts addContextNote`), a voice memo (`/capture/memo` with target query params), or an interview (`/capture/interview?target=person&id=...`). Task 3 (chat-to-interview): `components/chat.tsx` shows "Talk more about this" after an answer, launching an interview seeded with the last question (target=topic). Task 4 (Today follow-ups): a `ContextAdder` (target=commitment) plus a Plan panel that sets an intended date and links a person, stored in the `companion_state` overlay (migration 0012 adds `due_date` + `linked_person_id`) via `setFollowupTracking` with a read-merge-upsert (`patchCompanionState`) so the state buttons and the tracking do not clobber each other. This is the user's own tracking only: NO calendar/email/scheduling, NO external action. Telemetry: `context_add` (note/memo, with source + target), `companion_tracking`, the capture event's target attrs, and the interview's target.
- 2026-06-19: DEFERRED (planned, noted): the photo/reminisce minigame (the richest add-context surface, its own later PR); any real scheduling/calendar/email send; Google OAuth + connectors settings UI + single sign-on (a later connectors build); multi-user (the per-user RLS boundary is unchanged and verified, signups stay disabled); decay/salience + the freshness loop; and the system-wide deterministic-id hardening across the miner (Task 1a improves the people key but is not that full fix). Exploratory choices: the people identity key was chosen as normalize(first+' '+last) specifically to PRESERVE existing ids while making the basis first+last (avoiding a mass re-ID and reference repoint); first/last live in the loose `data` jsonb plus a display-split fallback (no schema change for them); the pending-rename overlay is id-keyed. The 6-dimension adversarial review (each finding double-verified) confirmed 0; of 6 rejected it surfaced 5 legitimate small issues that were fixed defensively anyway (the overlay re-fire + merge-duplicate via id-keying, setFollowupTracking validating the date before converting it, the interview start callback's missing `target` dependency that could drop a targeted brief, the person-brief explicit user_id scope, and the ContextAdder interview-link guard). Verification: `tsc --noEmit` clean after all edits; DB-free identity/target checks pass; the full live `scripts/check-context.ts` (28) and `scripts/check-companion.ts` (36) passed before the localized review fixes, and the Karalea overlay was demonstrated live. The local build and DB re-verification hit an environment wedge (ECANCELED on module read, Supabase connection exhaustion) from repeated test runs; CI provides the clean authoritative build. HANDOFFS: migration 0012 applies on merge; folding the new targeted captures into the graph (and the live interview round-trip) is blocked by the Anthropic usage limit until 2026-07-01.
